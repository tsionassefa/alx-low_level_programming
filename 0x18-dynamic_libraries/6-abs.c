#include "main.h"
/**
 *_abs - Prints the alphabet in lowercase.
 *@n : given number
 *
 *Return: Always 0 (Success)
 **/
int _abs(int n)
{
	if (n >= 0)
	return (n);
	else
	return (n * -1);
}

