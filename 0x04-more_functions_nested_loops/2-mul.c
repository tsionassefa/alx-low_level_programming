#include "main.h"
#include <stdio.h>

/**
 * mul - check the code for mul.
 * @a: First integer
 * @b: Second integer
 * Return: Always 0.
 */
int mul(int a, int b)
{
	int mul;
	mul = a * b;
	return (mul);
}
