#include "main.h"

/**
 *mul - Write a function that multiplies two integers.
 *@a: input character.
 *@b: input character.
 *Return: multiplcation of a and b.
 */
int mul(int a, int b)
{
	return (a * b);
}
