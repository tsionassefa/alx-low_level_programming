#include "main.h"
/**
 *print_line - checks if parameter is a number.
 *@n: input character.
 *Return: 1 if is a number, 0 in other case.
 */
void print_line(int n)
{
	for ( ; n > 0; n--)
	{
	_putchar(95);
	}
	_putchar('\n');
}
